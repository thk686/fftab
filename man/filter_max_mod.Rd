% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross.R
\name{filter_max_mod}
\alias{filter_max_mod}
\title{Filter for Maximum Magnitude in a \code{tidy_fft} Object}
\usage{
filter_max_mod(a)
}
\arguments{
\item{a}{A \code{tidy_fft} object containing FFT results.}
}
\value{
A tibble containing a single row corresponding to the frequency with the maximum magnitude.
}
\description{
This function filters a \code{tidy_fft} object to find the frequency with the maximum
magnitude (\code{mod}), while respecting the dimensional and complex/real-valued structure.
}
\details{
The function first converts the input to the polar representation (\code{'polr'}) using
\code{\link[=change_repr]{change_repr()}}, then filters out rows where all frequency dimensions (\verb{dim_*}) are zero
(for complex-valued input) or less than or equal to zero (for real-valued input).
}
\examples{
# Generate a tidy_fft object
a <- tidy_fft(c(1, 0, -1, 0))

# Compute cross-spectrum (self-correlation for simplicity)
cross_result <- cross_spec(a, a)

# Filter for maximum magnitude
max_mod_result <- filter_max_mod(cross_result)
print(max_mod_result)

}
\seealso{
\code{\link[=cross_spec]{cross_spec()}} for computing the cross-spectrum.
}
