% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross.R
\name{max_correlation_phase_units}
\alias{max_correlation_phase_units}
\title{Compute Maximum Correlation and Phase Shift in Original Units}
\usage{
max_correlation_phase_units(a, b, freq_scale = 1)
}
\arguments{
\item{a}{A \code{tidy_fft} object representing the first signal.}

\item{b}{A \code{tidy_fft} object representing the second signal.}

\item{freq_scale}{The scale of the original frequencies (e.g., sampling rate or spatial resolution).
Defaults to 1.}
}
\value{
A named numeric vector of length 2:
\itemize{
\item \code{max_correlation}: The maximum normalized correlation.
\item \code{shift_units}: The phase shift converted to the original time or space units.
}
}
\description{
This function computes the maximum correlation and corresponding phase shift
in the original time or space units from the cross-spectrum of two \code{tidy_fft} objects.
}
\examples{
# Example signals
x1 <- c(1, 0, -1, 0)
x2 <- c(0, 1, 0, -1)

# Compute tidy_fft for the signals
fft_a <- tidy_fft(x1)
fft_b <- tidy_fft(x2)

# Compute maximum correlation and shift in original units
result <- max_correlation_phase_units(fft_a, fft_b, freq_scale = 1)
print(result)  # Returns max correlation and shift in original units

}
\seealso{
\code{\link[=cross_spec]{cross_spec()}}, \code{\link[=filter_max_mod]{filter_max_mod()}}
}
