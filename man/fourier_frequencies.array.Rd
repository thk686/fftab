% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequency.R
\name{fourier_frequencies.array}
\alias{fourier_frequencies.array}
\title{Compute Fourier frequencies for multidimensional arrays}
\usage{
\method{fourier_frequencies}{array}(x)
}
\arguments{
\item{x}{A numeric array.}
}
\value{
A tibble where each column corresponds to the Fourier frequencies
of a dimension of the input array. The column names are \code{dim_1},
\code{dim_2}, ..., corresponding to each dimension.
}
\description{
Computes Fourier frequencies for each dimension of an array and returns
a tidy tibble with frequencies for all dimensions.
}
\examples{
# Compute Fourier frequencies for a 3D array
array_input <- array(1:27, dim = c(3, 3, 3))
fourier_frequencies(array_input)

# Compute Fourier frequencies for a 2D matrix
matrix_input <- matrix(1:9, nrow = 3, ncol = 3)
fourier_frequencies(matrix_input)

# Compute Fourier frequencies for a vector (1D case)
vector_input <- 1:8
fourier_frequencies(vector_input)
}
\seealso{
\code{\link[=fourier_frequencies.default]{fourier_frequencies.default()}}, \code{\link[tidyr:expand_grid]{tidyr::expand_grid()}}
}
